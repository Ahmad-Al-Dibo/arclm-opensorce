# Evaluation

Evaluation helpers live in `arclm.diagnostics` and are exported at top level:

- `calculate_metrics`
- `calculate_perplexity`
- `predict_top_k`
- `build_training_diagnostics_report`
- `export_metrics_to_json`
- `export_metrics_to_markdown`

Most evaluation support targets native ArcLM models.

