# Models

The native `ArcLM` model is a compact GPT-style decoder-only causal language model. `MiniGPT` is a backward-compatible alias.

External models are loaded through Hugging Face `AutoModelForCausalLM` when using `load_any_model` or `train_sft`.

See [Supported Models](../supported-models.md).

